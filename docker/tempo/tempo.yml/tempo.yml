server:
  http_listen_port: 3200

distributor:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: "tempo:4327"
        http:
          endpoint: "tempo:4328"

storage:
  trace:
    backend: local
    local:
      path: /tmp/tempo/blocks
